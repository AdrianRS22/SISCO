@model IEnumerable<SISCO.CapaLogica.Proveedor>

@{
    ViewBag.Title = "MostrarProveedor";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>MostrarProveedor</h2>

<p>
    
</p>
<table class="table table-responsive table-striped bg-white">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Direccion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Correo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Telefono)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Activo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaCreacion)
        </th>
        <th> </th>
        <th> </th>
        <th> </th>

    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Correo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefono)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Activo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaCreacion)
        </td>
        <td>
            @Html.ActionLink("Editar", "EditarProveedor", new { id=item.Id },new {@class= "btn btn-primary" }) |
            @Html.ActionLink("Detalles", "GetProveedor", new { id=item.Id }, new { @class = "btn btn-primary" }) |
            @Html.ActionLink("Eliminar", "EliminarProveedor", new { id=item.Id }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
